{
  "framework": null,
  "buildCommand": "DISABLE_ESLINT_PLUGIN=true npm run build && mv build/index.html build/app.html && cp -R website/css website/js website/images website/fonts build/ && cp website/*.html build/",
  "outputDirectory": "build",
  "rewrites": [
    { "source": "/map", "destination": "/app.html" },
    { "source": "/dashboard", "destination": "/app.html" }
  ]
}
